# 保留中/次フェーズ以降の項目

現時点(2026-08-19)で、ユーザーから明示的に「あとで」「次フェーズで」と
指示があり、まだ着手していないもの。着手する際は、ここに書いた前提が
今も正しいか(コードの現状と合っているか)を確認してから進めること。

## 1. VBAマクロの再実装(未定・要相談)

現状はUDF専用設計(VBAゼロ)。ユーザー本人が「また後日実装するかも
しれないが、今はなしでいい」と明言。**次にVBAの話が出たら、まず
「今回のUDF専用設計を維持したまま追加するのか、それとも設計方針自体を
見直すのか」を確認すること。** 安易に`RunPython`ベースの旧バッチ実行
(`swap_pricing/pricing_pipeline.py`、削除済み)のような、UDFセルを
静的値で上書きする方式を復活させないよう注意(過去に実際にF9で
計算されずRun mainボタンでUDFセルが上書きされる事故が発生している)。

## 2. LCH / TIBORなど他インデックスへの対応

現状はJSCC TONA(円OIS)のみ対応。「次フェーズ」として明示的に保留。
対応する場合、`curve.py`の`bootstrap_curve()`と`swap_pricer.py`の
インデックス生成部分(`ql.Tonar()`固定)を、インデックス種別を引数に
取る形に一般化する必要がある。RealTimeシートの`index`列(現状JSCC固定の
ドロップダウン)も選択肢を増やす必要がある。

## 3. `src/`配下の旧BOJ会合分析ツールのT+2起算日修正

本プロジェクト(`swap_pricing/`)とは別の既存ツール。ユーザーから
「今回の新規実装の土台としては使わない」と明言されており、**本プロジェクト
の作業として触ってはいけない**。修正自体は「後日」対応予定として記録
されているだけで、いつ着手するかは未定。

## (参考)既に決着済みで「保留」ではないもの

- **Historicalシートのグラフ10枚構想** — 初期構想はグラフ10枚
  (アウトライト/カーブ/フライ×複数系列)だったが、後の指示で
  「Outrightだけでよい、カーブ/フライはExcel上の単純計算でよい」と
  簡略化され、現状の3系列(2Y/5Y/10Y outright + 2s10sカーブ +
  2s5s10sフライ)構成に置き換わっている。グラフ自体は現状未実装だが、
  「10枚のグラフ」という初期要件はもう有効ではない。
- **15M/18M/21Mテナー** — `bootstrap_curve(include_odd_tenors=True)`で
  対応可能な状態で実装済み(デフォルトはFalseで除外)。デフォルトを
  変える/変えないは特に指示がないため現状維持。
- **カーブ/フライ列(spred/fly)のUDF化 vs Excel数式** — Excel数式化で
  決着済み([decisions.md](decisions.md)参照)。
- **ヒストリカルデータの配布方法(メール vs GCS)** — GCS公開オブジェクト
  方式で決着済み、実装・動作確認済み。
